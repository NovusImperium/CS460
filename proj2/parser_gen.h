#ifndef PARSER_GEN_H
#define PARSER_GEN_H

#include "defs.h"

// opens the rules file to read in and outputs the parser functions to the output file
extern inline bool generate(char *in, char *out);

#endif  // PARSER_GEN_H
